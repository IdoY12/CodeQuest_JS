import { StyleSheet } from "react-native";
import { colors } from "@/theme/theme";

export const learnNavigatorStyles = StyleSheet.create({
  header: { backgroundColor: colors.background },
  scene: { backgroundColor: colors.background },
});
