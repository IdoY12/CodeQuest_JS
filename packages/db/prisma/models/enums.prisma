/// Used by `DuelQuestion.difficulty` (three-band pool: JUNIOR / MID / SENIOR).
enum Difficulty {
  JUNIOR
  MID
  SENIOR
}

enum ExerciseType {
  MCQ
  PUZZLE
}

enum ExperienceLevel {
  JUNIOR
  MID
  SENIOR
}
